import { Jelly } from '@uiball/loaders';

export default function Loading() {
  return (
    <div className='mx-auto flex items-center justify-center'>
      <Jelly size={38} speed={0.9} color={'#B2AC88'} />
    </div>
  );
}
